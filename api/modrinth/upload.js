const { json, modrinth, readJson } = require('./_helpers');
module.exports = async (req, res) => { try { if (req.method !== 'POST') return json(res, 405, { error:'Method not allowed' }); const body = await readJson(req); const fileBuffer = Buffer.from(body.fileBase64 || '', 'base64'); if (!fileBuffer.length) throw new Error('Datei fehlt.'); const data = { name: (body.name || 'MC Toolkit Pack') + ' ' + (body.versionNumber || '1.0.0'), version_number: body.versionNumber || '1.0.0', changelog: 'Uploaded with MC Toolkit.', dependencies: [], game_versions: [body.gameVersion || '1.21.1'], version_type: 'release', loaders: [body.loader || 'fabric'], featured: false, project_id: body.projectId, file_parts: ['file'], primary_file: 'file' }; const form = new FormData(); form.append('data', JSON.stringify(data)); form.append('file', new Blob([fileBuffer]), body.filename || 'modpack.mrpack'); const r = await modrinth(req, '/version', { method:'POST', body: form, headers:{} }); json(res, 200, await r.json()); } catch(e) { json(res, e.status || 500, { error:e.message }); } };
